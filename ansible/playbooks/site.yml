---
- name: Deploy Agent Stack against external armory Keycloak
  hosts: all
  gather_facts: true
  roles:
    - role: preflight
      tags:
        - always
        - preflight

    - role: openbao_bootstrap
      tags:
        - always
        - openbao
        - bootstrap

    - role: agentstack_keycloak
      tags:
        - agentstack_keycloak

    # Next phases will add deploy roles in this order:
    # - agentstack_secrets
    # - agentstack
