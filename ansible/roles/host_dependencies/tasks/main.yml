---
- name: Install garrison host dependencies
  tags:
    - host_dependencies
    - host_prereqs
  ansible.builtin.dnf:
    name: "{{ garrison_host_dependency_packages }}"
    state: "{{ garrison_host_dependency_state }}"
    update_cache: "{{ garrison_host_dependency_update_cache | bool }}"
